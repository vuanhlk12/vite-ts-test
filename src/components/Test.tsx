import React, { FC } from "react";
import Logo from "../icons/favicon.svg?component";

const Test: FC = (props) => {
  return (
    <div>
      <Logo />
      1234
    </div>
  );
};

export default Test;
